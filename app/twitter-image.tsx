/**
 * X reads the same card.
 *
 * The platform prefers its own tag, but the artwork has no reason to differ,
 * so the Open Graph image is re-exported rather than maintained twice.
 */
export { default, alt, size, contentType } from './opengraph-image'
